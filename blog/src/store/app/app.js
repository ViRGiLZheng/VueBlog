import {siderMaps} from "../../router/index";

// initial state
const state = {
    siderMaps: siderMaps
};

// getters
const getters = {
    siderMaps: state => state.siderMaps
};


// actions
const actions = {
}

// mutations
const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations
};
